[package]
name = "radiogolha-android"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
radiogolha-core = { path = "../.." }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
