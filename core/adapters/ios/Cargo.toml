[package]
name = "radiogolha-ios"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]
name = "radiogolha_ios"

[dependencies]
radiogolha-core = { path = "../.." }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libc = "0.2"
